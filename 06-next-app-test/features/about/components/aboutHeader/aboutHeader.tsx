// Data Fetch

/* 
const getCustomFetch = ({path}) => {
  fetch({baseurl}/path, {
    method : 'GET'
  })
}
const postCustomPost = ({path, body}) => {
  fetch({baseurl}/path, {
    method : 'POST',
    body
  })
}

const getCustomFetch = ({path}) => {
  fetch({baseurl}/path, {
    method : 'Get'
  })
}

const userList = await getCustomFetch({'/user/list'|)

apiFetcher(FETCH_USER_KEY)
.setBody({})
.query({})
.fetch();
*/
